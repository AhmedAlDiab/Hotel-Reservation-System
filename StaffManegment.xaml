<UserControl x:Class="Hotel_Reservation_System.StaffManegment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hotel_Reservation_System"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="C:\Users\LOQ\OneDrive\Desktop\Hotel\Screenshot 2025-05-06 231106.png"/>
        </Grid.Background>
        <TextBlock Margin="317,70,254,0" TextWrapping="Wrap" Text="Staff Manegment" VerticalAlignment="Top" FontWeight="Bold" FontSize="20" Foreground="#D4AF37" FontFamily="Elephant"/>

        <DataGrid x:Name="staffmaneg" Height="200" Background="#FFFFFF" GridLinesVisibility="All"
AlternatingRowBackground="#FAF5EF"
   AutoGenerateColumns="True"
   CanUserAddRows="True"
   IsReadOnly="False"
   CanUserDeleteRows="True"
   SelectionMode="Single"
   ItemsSource="{Binding GetStaff}" Margin="254,117,237,117">
            <DataGrid.Resources>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="LightBlue"/>
                    <Setter Property="BorderBrush" Value="DarkBlue"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Black"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontSize" Value="14"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderBrush" Value="Gray"/>
                    <Setter Property="BorderThickness" Value="0.5"/>
                </Style>
            </DataGrid.CellStyle>




        </DataGrid>
        <Button Content="Remove" HorizontalAlignment="Left" Margin="594,151,0,0" VerticalAlignment="Top" FontSize="15" FontWeight="Heavy" Foreground="Gold" Width="90" Click="Remove" Height="42">

            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border  CornerRadius="2" BorderBrush="#D4AF37" BorderThickness="2.5" Margin="0,0,7,7">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button Content="Add" HorizontalAlignment="Left" Margin="591,225,0,0" VerticalAlignment="Top" FontSize="15" FontWeight="Heavy" Foreground="Gold" Width="91" Click="add" Height="42">

            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border  CornerRadius="2" BorderBrush="#D4AF37" BorderThickness="2.5" Margin="0,0,0,5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>

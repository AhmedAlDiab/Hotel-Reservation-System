<UserControl x:Class="Hotel_Reservation_System.ADDROOM"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hotel_Reservation_System"
             mc:Ignorable="d" 
             d:DesignHeight="375" d:DesignWidth="300">
    <Grid Margin="10" Background="Purple">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,5">
            <TextBlock Text="Room ID:"/>
            <TextBox Name="txtRoomID"/>

            <TextBlock Text="Capacity:"/>
            <TextBox Name="txtCapacity"/>

            <TextBlock Text="Price per Night:"/>
            <TextBox Name="txtPrice"/>

            <TextBlock Text="Room Type:"/>
            <ComboBox Name="comboRoomType" />

            <TextBlock Text="Bed Type:"/>
            <ComboBox Name="comboBedType"/>

            <TextBlock Text="Meal Plan:"/>
            <ComboBox Name="comboMealPlan"/>

            <TextBlock Text="Room:"/>
            <ComboBox Name="comboRoomClass">
                <ComboBoxItem>Standard</ComboBoxItem>
                <ComboBoxItem>Deluxe</ComboBoxItem>
            </ComboBox>

            <TextBlock Text="Discount (for Deluxe):"/>
            <TextBox Name="txtDiscount" Text="0"/>

            <CheckBox Name="chkAvailable" Content="Is Available" Margin="0,5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Top">
            <Button Content="Confirm" Click="confirmroome_Click" Margin="0,10,0,5"/>
           
        </StackPanel>
    </Grid>
</UserControl>
